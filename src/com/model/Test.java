package com.model;

import java.io.FileNotFoundException;

import com.io.ReadData;
import com.io.WriteData;

public class Test {
	/*
	 * public static void main(String[] args) { System.out.println("Starting");
	 * System.out.println(ReadData.readFiles()); UserFile uf1 = new
	 * UserFile("newfile.txt"); uf1.setData(ReadData.readFileData());
	 * WriteData.writeFile(uf1); UserFile uf; try { uf =
	 * ReadData.readFile("notexists.txt"); } catch (FileNotFoundException e) {
	 * System.out.println(e.getMessage()); } System.out.println(uf1);
	 * //uf.setData("I added some stuff to it yo!"); //WriteData.writeFile(uf); }
	 */
}
